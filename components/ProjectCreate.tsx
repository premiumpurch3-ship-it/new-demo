 "use client";
import {useState} from "react"; import {useRouter} from "next/navigation";
export default function ProjectCreate({clients}:{clients:{id:string;name:string}[]}){const [open,setOpen]=useState(false),[busy,setBusy]=useState(false),[error,setError]=useState("");const router=useRouter();
 async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setError("");const body=Object.fromEntries(new FormData(e.currentTarget).entries());const r=await fetch("/api/projects",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(body)});const d=await r.json();if(!r.ok)setError(d.error||"Create failed");else{setOpen(false);router.refresh()}setBusy(false)}
 return <><button className="btn primary" disabled={!clients.length} onClick={()=>setOpen(true)}>+ New project</button>{open&&<div style={{position:"fixed",inset:0,background:"rgba(0,0,0,.7)",display:"grid",placeItems:"center",zIndex:20,padding:20}}><div className="card" style={{width:"min(650px,100%)"}}><div className="section-head"><h2>New project</h2><button className="btn" onClick={()=>setOpen(false)}>Close</button></div><form className="form" onSubmit={submit}><div className="field"><label>Client</label><select className="input" name="client_id" required>{clients.map(c=><option key={c.id} value={c.id}>{c.name}</option>)}</select></div><div className="form-grid"><div className="field"><label>Project name</label><input className="input" name="name" required/></div><div className="field"><label>Slug</label><input className="input" name="slug" placeholder="my-project"/></div></div><div className="form-grid"><div className="field"><label>Framework</label><select className="input" name="framework"><option>Next.js</option><option>React</option><option>Vite</option><option>Other</option></select></div><div className="field"><label>Hosting</label><select className="input" name="hosting"><option>Vercel</option><option>Cloudflare Pages</option><option>Cloudflare Workers</option><option>Other</option></select></div></div><div className="field"><label>GitHub repository URL (optional)</label><input className="input" name="repo_url" placeholder="https://github.com/owner/repository"/></div><div className="field"><label>Repository branch</label><input className="input" name="repo_branch" defaultValue="main"/></div>{error&&<div className="error">{error}</div>}<button className="btn primary" disabled={busy}>{busy?"Creating…":"Create project"}</button></form></div></div>}</>
}