 "use client";
import {useState} from "react"; import {useRouter} from "next/navigation";
export default function ClientActions({mode,id}:{mode:"create"|"delete";id?:string}){
 const [open,setOpen]=useState(false); const [busy,setBusy]=useState(false); const [error,setError]=useState(""); const router=useRouter();
 if(mode==="delete") return <button className="btn danger" onClick={async()=>{if(!confirm("Delete this client and its projects?"))return;setBusy(true);const r=await fetch("/api/clients/"+id,{method:"DELETE"});if(!r.ok){const d=await r.json();alert(d.error||"Delete failed")}router.refresh();setBusy(false)}}>{busy?"…":"Delete"}</button>;
 async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setError("");const fd=new FormData(e.currentTarget);const body=Object.fromEntries(fd.entries());const r=await fetch("/api/clients",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(body)});const d=await r.json();if(!r.ok)setError(d.error||"Create failed");else{setOpen(false);router.refresh()}setBusy(false)}
 return <><button className="btn primary" onClick={()=>setOpen(true)}>+ New client</button>{open&&<div style={{position:"fixed",inset:0,background:"rgba(0,0,0,.7)",display:"grid",placeItems:"center",zIndex:20,padding:20}}><div className="card" style={{width:"min(600px,100%)"}}><div className="section-head"><h2>Create client</h2><button className="btn" onClick={()=>setOpen(false)}>Close</button></div><form className="form" onSubmit={submit}><div className="form-grid"><div className="field"><label>Name</label><input className="input" name="name" required/></div><div className="field"><label>Company</label><input className="input" name="company"/></div></div><div className="field"><label>Email</label><input className="input" type="email" name="email" required/></div><div className="field"><label>Phone</label><input className="input" name="phone"/></div>{error&&<div className="error">{error}</div>}<button className="btn primary" disabled={busy}>{busy?"Creating…":"Create client"}</button></form></div></div>}</>
}