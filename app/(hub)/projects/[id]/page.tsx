import {notFound} from "next/navigation"; import {query} from "@/lib/db"; import DeployButton from "@/components/DeployButton";
export default async function ProjectPage({params}:{params:Promise<{id:string}>}){const {id}=await params;const p=await query<any>("select p.*,c.name client_name,c.email client_email from projects p join clients c on c.id=p.client_id where p.id=$1",[id]);if(!p.rows[0])notFound();const d=await query<any>("select id,status,provider,message,created_at from deployments where project_id=$1 order by created_at desc limit 15",[id]);const x=p.rows[0];
return <main className="content"><div className="section-head"><div><h1>{x.name}</h1><div className="muted">{x.client_name} · {x.slug}</div></div><DeployButton projectId={x.id} hosting={x.hosting}/></div>
<div className="grid two"><div className="card"><h2>Configuration</h2><div className="grid" style={{marginTop:16}}><div><span className="muted small">Framework</span><div>{x.framework}</div></div><div><span className="muted small">Hosting</span><div>{x.hosting}</div></div><div><span className="muted small">Production URL</span><div>{x.production_url||"Not connected"}</div></div><div><span className="muted small">Client</span><div>{x.client_name} · {x.client_email}</div></div></div></div>
<div className="card"><h2>Deployment readiness</h2><p className="muted small">Real provider deployment is enabled when the matching provider credentials are configured in Vercel.</p><div className="notice">No fake deployment is reported. If a provider token is missing, the action returns a clear setup error.</div></div></div>
<div className="card" style={{marginTop:16}}><h2>Deployment history</h2><div className="table-wrap" style={{marginTop:14}}><table><thead><tr><th>Status</th><th>Provider</th><th>Message</th><th>Created</th></tr></thead><tbody>{d.rows.map((z:any)=><tr key={z.id}><td><span className={"badge "+(z.status==="SUCCESS"?"success":z.status==="FAILED"?"danger":"warning")}>{z.status}</span></td><td>{z.provider}</td><td>{z.message||"—"}</td><td>{new Date(z.created_at).toLocaleString()}</td></tr>)}{!d.rows.length&&<tr><td colSpan={4}><div className="empty">No deployment attempts yet.</div></td></tr>}</tbody></table></div></div>
</main>}
