import {query} from "@/lib/db";
export default async function Deployments(){const r=await query<any>("select d.id,d.status,d.provider,d.message,d.created_at,p.name project_name,c.name client_name from deployments d join projects p on p.id=d.project_id join clients c on c.id=p.client_id order by d.created_at desc limit 100");
return <main className="content"><div><h1>Deployments</h1><div className="muted">Audit trail for every deployment attempt.</div></div><div className="table-wrap" style={{marginTop:24}}><table><thead><tr><th>Project</th><th>Client</th><th>Status</th><th>Provider</th><th>Message</th><th>Time</th></tr></thead><tbody>{r.rows.map(x=><tr key={x.id}><td>{x.project_name}</td><td>{x.client_name}</td><td><span className={"badge "+(x.status==="SUCCESS"?"success":x.status==="FAILED"?"danger":"warning")}>{x.status}</span></td><td>{x.provider}</td><td>{x.message||"—"}</td><td>{new Date(x.created_at).toLocaleString()}</td></tr>)}{!r.rows.length&&<tr><td colSpan={6}><div className="empty">No deployment activity.</div></td></tr>}</tbody></table></div></main>}
