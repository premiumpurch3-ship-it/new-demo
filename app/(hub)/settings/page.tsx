export default function Settings(){return <main className="content"><h1>Settings</h1><div className="muted">Deployment Hub configuration.</div><div className="grid two" style={{marginTop:24}}><div className="card"><h2>Provider configuration</h2><p className="muted small">Secrets are intentionally server-side. Configure them in Vercel Project Settings → Environment Variables.</p><div className="grid"><div className="notice">DATABASE_URL — required for PostgreSQL</div><div className="notice">AUTH_SECRET — required for secure sessions</div><div className="notice">VERCEL_TOKEN — required for real Vercel deployments</div><div className="notice">VERCEL_TEAM_ID — optional for team deployments</div></div></div><div className="card"><h2>Architecture</h2><p className="muted">Next.js App Router serves the UI and server-side API routes. PostgreSQL stores application data. Vercel hosts the application and can also be the deployment provider for managed client projects.</p></div></div></main>}
